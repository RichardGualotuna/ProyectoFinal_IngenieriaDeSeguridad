# ============================================================================
# VARIABLES DE ENTORNO - Sistema de Facturación Electrónica
# Copiar este archivo como .env y configurar los valores
# ============================================================================

# Entorno
FLASK_ENV=development
FLASK_APP=app.py

# Seguridad
SECRET_KEY=tu-clave-secreta-muy-segura-aqui-cambiar-en-produccion
JWT_SECRET_KEY=tu-jwt-secret-key-diferente-de-la-anterior

# Base de Datos PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=richard
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=richard_db

# Criptografía (Generar con: python -c "import base64, os; print(base64.b64encode(os.urandom(32)).decode())")
AES_MASTER_KEY=tu-clave-aes-base64-de-32-bytes-aqui

# CORS (separado por comas)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# SRI Ecuador
AMBIENTE_SRI=PRUEBAS
